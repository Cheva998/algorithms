print("hello world!")
print(4 and 5) -- first operand is true, so returns second
print(nil and "Lua") -- first operand is false, so returns first
print(4 or 5) -- first operand is true, so returns first
print(nil or "Lua") -- first operand is false, so returns second
print(arg[0]) -- prints the script path
